<IfModule mod_rewrite.c>
    RewriteEngine On

    # If the requested resource doesn't map to a real file or directory, route to index.php
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [L]
</IfModule>
